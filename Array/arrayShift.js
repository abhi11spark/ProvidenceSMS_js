var arr = [77, 88, 99]

console.log(arr); //prints the original array
console.log(arr.shift()); //removes the first value/element of the array and prints/return length of the array
console.log(arr); //first element will be removed and prints/return the remaining array